<% unless user_signed_in? %>
  <div class="d-flex justify-content-center mt-5">
    <div class="d-flex flex-row mt-5">
    </div>
  </div>
  <div class="d-flex justify-content-center">
    欢迎来到微信名片后台
  </div>
  <div class="d-flex justify-content-center mt-5">
    <div class="col-md-6">
      <div class="d-flex justify-content-center">
        <div class="d-flex justify-content-center mb-4 mr-4">
          <%= link_to "登录", new_user_session_path, class: "btn btn-primary" %>
        </div>
        <div class="d-flex justify-content-center mb-4">
          <%= link_to "注册", new_user_registration_path,  class: "btn btn-primary" %>
        </div>
      </div>
    </div>
  </div>
<% else %>
  <div class="d-flex flex-column">
    <div class="mb-1"><%= link_to "基本信息", basic_path, class: "btn btn-sm btn-primary" %></div>
    <div class="mb-1"><%= link_to "评论设置", comments_path, class: "btn btn-sm btn-primary" %></div>
    <div class="mb-1"><%= link_to "地图设置", location_path, class: "btn btn-sm btn-primary" %></div>
  </div> 
<% end -%>
