<% @product_likes.each_with_index do |product_like, index| %>
  <div class="d-flex justify-content-between">
    <div>
      <%= params[:page] ? (index + 1 + (params[:page].to_i-1)*10) : (index+1) %>.
      <%= product_like.guest.nickname %>
    </div>
    <div>
      <%= link_to '删除', delete_product_product_like_path(@product, product_like), remote: true, class: "badge badge-primary" %>
    </div>
  </div>
<% end %>
<div class="d-flex justify-content-between mt-2">
  <div>
    <%= link_to '返回商品管理', products_path, class: "btn btn-primary btn-sm" %>
  </div>
  <div><%= paginate @product_likes %></div>
</div>
<%= render 'shared/confirm_modal' %>
